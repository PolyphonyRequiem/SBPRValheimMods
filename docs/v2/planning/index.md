# index — docs/v2/planning

| file | purpose |
|------|---------|
| README.md | Human orientation for v2 planning |
| index.md | This manifest |
| requirements.md | Locked v2 cartography requirements (Surveyor's Table, Local Maps, Cartographer's Kit) |
| cartography-impl-spec.md | Per-feature buildable implementation spec — acceptance criteria, vanilla hooks, feature-folder placement, SpecCheck rows (gates the 3 impl cards) |
| marker-signs-impl-spec.md | Per-feature buildable spec for Marker Signs + WorldPin — 4 additive marker pieces, Shift+E gesture, derive-by-scan reconcile, SpecCheck +4 rows (the pin substrate the cartography viewer consumes) |
| ancient-portal-impl-spec.md | Per-feature buildable spec for Portal Seed → Ancient Portal — two-prefab cairn-pattern (Seed item + Hammer-placed portal piece), additive TeleportWorld kitbash, the PortalPrefabHash pairing gotcha, 15s grow timer, overhead jump-trigger, 11 named ATs, SpecCheck +2 rows |
| map-provider-binding-impl-spec.md | Per-feature buildable spec for the equipped-local-map PROVIDER BINDING state machine (most-recent-equipped wins; unbinds on re-equip / leave-inventory / death) + the carry-state circular minimap disc that reuses the §2H.1 viewer at minimap scale (inherits #159 clip), nomap-on gated, with the disc-centring question (§7 'player-centered' vs §2H.1 'table-centred') flagged OPEN for Daniel. SpecCheck +0 rows |
| local-map-mkey-open-impl-spec.md | Per-feature buildable spec for moving the bound-local-map OPEN gesture from E (Use) → M, removing the E-to-open path, swapping the HUD prompt token ($KEY_Use → $KEY_Map), and — the load-bearing part — SBPR OWNING the M edge in nomap-OFF via a `Minimap.Update` consume-prefix (`ResetButtonStatus`) so our viewer opens without vanilla's full map stacking. Realizes the 🟢 DECIDED M-key model (map-provider-model.md §1); supersedes cartography-impl-spec §2F/§2G open-input + requirements AT-MAP-EQUIP. Closes issue 3 / card t_f9a04fda. 11 named AT-MKEY-* tests. SpecCheck +0 rows |
| local-map-disc-name-hint-impl-spec.md | Per-feature buildable spec relocating the `[M]` open-hint from the floating bottom-centre screen overlay to a caption UNDER the carry minimap disc, co-located with the bound map's NAME ("Local map for <Table>"), and widening its visibility from equipped-only to disc-visibility (provider bound + nomap-ON) so it matches when M actually opens the map. Net-new addendum to the locked map-provider-model.md §2 minimap model (§2.1); supersedes local-map-mkey-open-impl-spec.md §5's bottom-centre prompt PLACEMENT (the $KEY_Map token stands). Daniel's 2026-06-19 v0.2.27 playtest refinement; card t_338f723b. AT-MAPNAME-UNDER-DISC / AT-MKEY-HINT-COLOCATED / AT-HINT-VISIBILITY + regressions. SpecCheck +0 rows |
| local-map-provider-persist-impl-spec.md | Buildable spec fixing the SESSION-SCOPED provider binding (BUG card t_5fc02f00): the minimap disc vanishes on logout/login even though the imprinted Local Map is still carried, returning only on the next equip. Reverses the one drifted carve-out (map-provider-binding-impl-spec.md §3.1 "client-only, session-scoped … null on relog") back to the LOCKED requirement AT-MAP-DURABLE (binding persists while carried) — design §3.2 was silent on relog, never sanctioned it. Fix = a one-shot cold-start carry re-derivation LATCH in LocalMapController (re-derive `_provider` from the first carried *imprinted* map once per session, gated so in-session re-pickup still stays unbound per §3.4) — NO new m_customData key, NO LocalMap.cs change, NO new Harmony patch. Rejects the card-recommended (a) persist-identity flag (permanent wire contract + cross-player stale-state leak) and (a′) monotonic stamp, documenting (a′) as the reversible upgrade if Daniel ever wants exact across-relog most-recent in the rare multi-carried-unequipped case. Equipped-at-logout already self-heals via vanilla EquipInventoryItems (decomp-grounded). The §3.1 + design §3.1/§3.2 content flip rides the engineer's CODE PR (same-PR-as-code); this spec-pass PR adds only the doc + breadcrumbs. AT-PERSIST-CARRY/EQUIP-SELFHEAL/MULTI/UNBIND-INTACT/BLANK/NOMAPOFF/SPEC. SpecCheck +0. Clean-side (ADR-0001). **status: proposed** — Daniel gates doc-review + in-game ATs; engineer-ui child builds it. |
| local-map-biome-indicator-impl-spec.md | Per-feature buildable spec adding a vanilla-style current-biome NAME readout (Path A — Daniel 2026-06-19 "A and architect") to BOTH cartography surfaces. Disc: a biome line in the under-disc caption stack (name / biome / [M]-hint). Modal: a fixed current-biome readout under the title (cursor-hover deferred — passive modal). ONE MapSurface.CurrentBiomeNameOrNull() helper feeds both (Player.GetCurrentBiome() → $biome_* token, None/unlocalized guarded against literal leak). Computed live — NO SurveyData wire change (not Path B-baked), no vanilla m_biomeName*/Minimap/material/root mutation (nomap enforced). Extends the PR #205 caption infra (t_26bba85b); net-new addendum to map-provider-model.md §2.2. Card t_caf0f1cf. AT-BIOME-MINIMAP / AT-BIOME-MODAL / AT-BIOME-SHARED / AT-BIOME-CLEAN + regressions. SpecCheck +0 rows |

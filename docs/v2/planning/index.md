# index — docs/v2/planning

| file | purpose |
|------|---------|
| README.md | Human orientation for v2 planning |
| index.md | This manifest |
| requirements.md | Locked v2 cartography requirements (Surveyor's Table, Local Maps, Cartographer's Kit) |
| cartography-impl-spec.md | Per-feature buildable implementation spec — acceptance criteria, vanilla hooks, feature-folder placement, SpecCheck rows (gates the 3 impl cards) |
| marker-signs-impl-spec.md | Per-feature buildable spec for Marker Signs + WorldPin — 4 additive marker pieces, Shift+E gesture, derive-by-scan reconcile, SpecCheck +4 rows (the pin substrate the cartography viewer consumes) |
| ancient-portal-impl-spec.md | Per-feature buildable spec for Portal Seed → Ancient Portal — two-prefab cairn-pattern (Seed item + Hammer-placed portal piece), additive TeleportWorld kitbash, the PortalPrefabHash pairing gotcha, 15s grow timer, overhead jump-trigger, 11 named ATs, SpecCheck +2 rows |
| map-provider-binding-impl-spec.md | Per-feature buildable spec for the equipped-local-map PROVIDER BINDING state machine (most-recent-equipped wins; unbinds on re-equip / leave-inventory / death) + the carry-state circular minimap disc that reuses the §2H.1 viewer at minimap scale (inherits #159 clip), nomap-on gated, with the disc-centring question (§7 'player-centered' vs §2H.1 'table-centred') flagged OPEN for Daniel. SpecCheck +0 rows |
| local-map-mkey-open-impl-spec.md | Per-feature buildable spec for moving the bound-local-map OPEN gesture from E (Use) → M, removing the E-to-open path, swapping the HUD prompt token ($KEY_Use → $KEY_Map), and — the load-bearing part — SBPR OWNING the M edge in nomap-OFF via a `Minimap.Update` consume-prefix (`ResetButtonStatus`) so our viewer opens without vanilla's full map stacking. Realizes the 🟢 DECIDED M-key model (map-provider-model.md §1); supersedes cartography-impl-spec §2F/§2G open-input + requirements AT-MAP-EQUIP. Closes issue 3 / card t_f9a04fda. 11 named AT-MKEY-* tests. SpecCheck +0 rows |
| local-map-disc-name-hint-impl-spec.md | Per-feature buildable spec relocating the `[M]` open-hint from the floating bottom-centre screen overlay to a caption UNDER the carry minimap disc, co-located with the bound map's NAME ("Local map for <Table>"), and widening its visibility from equipped-only to disc-visibility (provider bound + nomap-ON) so it matches when M actually opens the map. Net-new addendum to the locked map-provider-model.md §2 minimap model (§2.1); supersedes local-map-mkey-open-impl-spec.md §5's bottom-centre prompt PLACEMENT (the $KEY_Map token stands). Daniel's 2026-06-19 v0.2.27 playtest refinement; card t_338f723b. AT-MAPNAME-UNDER-DISC / AT-MKEY-HINT-COLOCATED / AT-HINT-VISIBILITY + regressions. SpecCheck +0 rows |
| local-map-provider-persist-impl-spec.md | Buildable spec fixing the SESSION-SCOPED provider binding (BUG card t_5fc02f00): the minimap disc vanishes on logout/login even though the imprinted Local Map is still carried, returning only on the next equip. Reverses the one drifted carve-out (map-provider-binding-impl-spec.md §3.1 "client-only, session-scoped … null on relog") back to the LOCKED requirement AT-MAP-DURABLE (binding persists while carried) — design §3.2 was silent on relog, never sanctioned it. Fix = a one-shot cold-start carry re-derivation LATCH in LocalMapController (re-derive `_provider` from the first carried *imprinted* map once per session, gated so in-session re-pickup still stays unbound per §3.4) — NO new m_customData key, NO LocalMap.cs change, NO new Harmony patch. Rejects the card-recommended (a) persist-identity flag (permanent wire contract + cross-player stale-state leak) and (a′) monotonic stamp, documenting (a′) as the reversible upgrade if Daniel ever wants exact across-relog most-recent in the rare multi-carried-unequipped case. Equipped-at-logout already self-heals via vanilla EquipInventoryItems (decomp-grounded). The §3.1 + design §3.1/§3.2 content flip rides the engineer's CODE PR (same-PR-as-code); this spec-pass PR adds only the doc + breadcrumbs. AT-PERSIST-CARRY/EQUIP-SELFHEAL/MULTI/UNBIND-INTACT/BLANK/NOMAPOFF/SPEC. SpecCheck +0. Clean-side (ADR-0001). **status: proposed** — Daniel gates doc-review + in-game ATs; engineer-ui child builds it. |
| local-map-biome-indicator-impl-spec.md | Per-feature buildable spec adding a vanilla-style current-biome NAME readout (Path A — Daniel 2026-06-19 "A and architect") to BOTH cartography surfaces. Disc: a biome line in the under-disc caption stack (name / biome / [M]-hint). Modal: a fixed current-biome readout under the title (cursor-hover deferred — passive modal). ONE MapSurface.CurrentBiomeNameOrNull() helper feeds both (Player.GetCurrentBiome() → $biome_* token, None/unlocalized guarded against literal leak). Computed live — NO SurveyData wire change (not Path B-baked), no vanilla m_biomeName*/Minimap/material/root mutation (nomap enforced). Extends the PR #205 caption infra (t_26bba85b); net-new addendum to map-provider-model.md §2.2. Card t_caf0f1cf. AT-BIOME-MINIMAP / AT-BIOME-MODAL / AT-BIOME-SHARED / AT-BIOME-CLEAN + regressions. SpecCheck +0 rows |
| bear-hide-tent-collider-fit-impl-spec.md | Buildable fix for the Bear Hide Tent collider defect (card t_439f2351 defect 1, Daniel 2026-06-26 "the collision mesh has no relationship to the tent mesh"). GROUNDED firsthand: the shipped bundle mesh AABB centres at root-local (4.49,2.38,1.46) but is attached at zero, so the canopy renders ~4.7 m off the box collider. CORRECTS the card's proposed "measure a box to the AABB" fix — the real defect is a wrong collider TYPE (the as-built threw away the donor's open-sided MeshCollider for a solid 8×4.9×6.9 box that bars walk-under and inverts the §2 open-canopy cover intent). Fix = graft the donor's concave MeshCollider (the vanilla TraderTent ships exactly this, proving it's safe), seated to the MEASURED mesh foot/centre (Signs/MarkerSigns Assets.Measure* pattern, NOT the hand-guessed SurveyorsTable box), + demote the shell box to a thin ground pad (Ancient Portal walk-up philosophy). engineer-systems. AT-COLLIDER-FIT/WALK-UNDER/UNDERROOF/PLANT/HIT/BUILD. SpecCheck +0. |
| bear-hide-tent-triad-build-impl-spec.md | Buildable spec for the two UNBUILT triad pieces (card t_439f2351 defects 2,3): special bedroll (piece_sbpr_bedroll — vanilla Bed + prefab-gated Bed.CheckExposure cover-clause relax keeping underRoof + skip-night-NO-spawn) and covered camp fire (piece_sbpr_camp_fire — small Fireplace + Heat EffectArea, inverting the cairn's strip-Heat machinery to KEEP it). Closes design §5 knobs: Q3 Spade / Q5 three-pieces / Q6 keep-underRoof RESOLVED by grounded lean; Q4 (storm-proofing) + Q7 (Inspired) PROPOSED Daniel-gated. 🔴 Q7 sequencing correction: the Inspired buff is NOT built (zero code) so the bedroll grants vanilla SE_Rested now, Inspired deferred to beautification graduation. engineer-systems. AT-BEDROLL-SLEEP/NOFIRE/WET/COMFORT/VANILLA + AT-CAMPFIRE-PLACE + AT-TRIAD-BUILD. SpecCheck +2 (bedroll + camp fire). |
| live-update-cartography-impl-spec.md | Per-feature buildable spec for the LOCKED live-update WRITE axis (design map-provider-model.md §3.2a/§4.0a/§5/§6, PR #266, Daniel 2026-06-24). Kit-worn field reveal genuinely MUTATES every carried imprinted in-region Local Map's stored m_customData artifact (plural write-set, OR-merge windowed global fog → blob, dirty-checked) plus the vanilla global — SUPERSEDING the §2I render-overlay (issue 5/PR #131) that never mutated storage. Perf model LOCKED = direct-blob-mutation with dirty-check (justified vs sub-KB 64 m blob; in-RAM working-set documented as deferred optimization). Surveyor's Table local→ingest on Use (grid-cell-keyed bound-map adoption, §5). Confirms viewer auto-reflects the mutated blob via its existing 0.25 s ReadSurvey poll → no render change. New LiveFieldWrite.cs + LocalMap.WriteSurveyBlob + SurveyData.MergeFrom(out changed) + shared MinimapFog.ReadExplored. Card t_d46b3398; engineer child t_9c54d492. AT-LIVE-WRITE-1/NOKIT/MULTI/OUTREGION/GLOBAL/PERSIST/SUPERSEDE + AT-INGEST-1/REBUILD. SpecCheck +0 rows |
| homestead-stone-progression-spec.md | Normative S2 feature specification for the confirmed Niflheim Homestead progression proof: five prioritized journeys, 27 functional requirements, Stone Facets, BP-driven Tree advancement, 20 authored nodes (13 executable / seven unavailable), revocation/Facet Credit, relationship release/recovery, remote-shaped commands, success criteria, and explicit exclusions. Accepted by Daniel; task creation and implementation remain separately unauthorized. |
| homestead-stone-progression-research.md | Current-code and feasibility grounding for S2: existing Homestead identity/ZDO substrate, absent progression/auth/transaction capabilities, confirmed vanilla/repository seams, unsafe assumptions, tuning boundary, and mandatory P0 authenticated-identity + atomic AP receipt gate. |
| homestead-stone-progression-data-model.md | Logical S2 state contract: stable IDs; separate Stone, policy-driven account-authority, and character aggregates; Stone Facets; current-build content; operation receipts; derived activation/read models; 20-node roster; invariants; transitions; and recovery. |
| homestead-stone-progression-contracts.md | Server-authoritative S2 application contracts: authenticated/revisioned/idempotent relationship, activity, Facet commitment, BP-driven node development/Tree advancement, purchase, revocation, query, and provider boundaries plus stable rejection vocabulary and hostile-client tests. |
| homestead-stone-progression-plan.md | SBPR-native Spec Kit plan for S2: technical context, constitution pass, proposed deep-module source boundary, Gate A + Tracers 1–9 with named acceptance and dependencies, test/conformance strategy, separate-verifier gate, and explicit stop before implementation. |
| homestead-stone-progression-tasks.md | Accepted S2 task decomposition (PR #297): current-main/v1 prerequisite, blocking Gate A, dependency-ordered vertical slices, exact planned paths, named acceptance, four post-grammar parallel family branches, paired independent verification, and explicit stop before implementation. |
| homestead-resource-delivery-spec.md | Proposed normative feature specification for account-pair Stone loyalty, weekly/daily participation, offline parallel outcome growth, multiplier-aware AP, any-Tree Resource Delivery nodes, shared Stone Stock, and the 10 Wood + 10 Stone Humble Homesteader fixture; 24 requirements with exact acceptance mapping and first-slice roster 21/14/7. |
| homestead-resource-delivery-research.md | Current-source grounding for Resource Delivery: existing relationship/AP/BP/receipt seams, absent Connection/participation/Stock behavior, proposed supersessions, threat model, and mandatory time/fan-out/inventory/capacity spikes. |
| homestead-resource-delivery-data-model.md | Proposed logical state contract for Connection, account–Stone Participation, Donation Menu, Resource Delivery meter/bundle, Stone Stockpile, AP receipt extension, content, transitions, lifecycle, and recovery. |
| homestead-resource-delivery-contracts.md | Proposed authenticated application contracts for qualifying-link lifecycle, upkeep donation, daily-practice evidence, BP node development, AP/contribution reconciliation, Stock permissions/withdrawal, reads, providers, and stable rejections. |
| homestead-resource-delivery-plan.md | Proposed approval-gated plan: constitution check, current-truth guard, deterministic-time/fan-out Gate A, five vertical tracers, exact AT-RD-001..024 acceptance set, writer≠verifier gate, and no tasks/implementation. |
| server-characters-accounts-discord.md | DISCUSS (architect, card t_67ba7117, 2026-07-16): charts the design space for authoritative server-side characters, account management, and optional Discord linking BEFORE any aggregate/OAuth/bot/migration is committed. Grounds every claim against the shipped identity floor (data-model AccountId/CharacterId, research candidate A/E, PR #317 transport-bound identity). Ships a ubiquitous-language glossary; three architecture shapes (Thin platform-rooted / **Server-local account + Discord-as-linked-community-identity [LEAN]** / Federated OAuth-as-credential [out unless opted-in]); account/character/link lifecycle tables; a migration/recovery model (journal-replay + credential-rebind, no time-travel); a minimum vertical slice + explicit not-yet scope; and an 11-row threat sweep. Two load-bearing leans: account root = server-minted subject with platform as first credential (candidate E), and **Discord role state is NEVER an input to in-game authorization** (read/notify only). status: idea — non-locking; 8 reserved Daniel decision clusters (2A account-root + 3D Discord-boundary lock first); portability-scope + production-account-provider routed to RE. No implementation, no OAuth, no bot automation, no spec lock. |
| account-identity-pilot-spec.md | Proposed normative contract for the three-week closed cooperative pilot: server-minted AccountId/CharacterId, one Gate-0-proven transport provider, HMAC-only allowlist/credential/profile lookup, one pending-or-active account admission, manual operations/privacy, and federation explicitly deferred. |
| account-identity-pilot-research.md | Current-code, external-landscape, privacy, performance, and feasibility grounding; identifies provider-shaped AccountId, profile-shaped CharacterId, raw PlatformId receipt digest, scan-per-operation lookup, and exact-provider Gate 0 as the blocking gaps. |
| account-identity-pilot-data-model.md | Logical pilot state for HMAC-only allowlist, account, credential, character binding, pending/active admission lease, indexed journal, operator views, retention holds, key census/retirement, mutation fences, pilot closure, artifact catalog/purge evidence, deletion, and whole-fixture reset fallback. |
| account-identity-pilot-contracts.md | Provider/profile adapter, account/character admission, lease/session, provider-free gameplay principal, operator/privacy, receipt, query, rejection, logging, and future-provider boundaries. |
| account-identity-pilot-plan.md | Three-week dependency/tracer plan: provider Gate 0, account+credential, character/session, gameplay receipt scrub, allowlist-only local bootstrap, operator/privacy/artifact lifecycle, joined-client final gate, safe parallelism, and explicit no-tasks/no-implementation stop. |
| account-identity-pilot-gate0-evidence.md | IAP-001 Gate 0 executed evidence (t_b8570a62): names Steamworks as the one pilot backend, proves server-observed (never payload) principal, rejection vocabulary, reconnect/restart subject stability, the protected no-echo/owner-only/allowlist-only provisioning input, clean Niflheim auth logs, and the upstream raw-subject inventory fail-closed rule. Engine-free CLEAN-side core (`Adapters/Identity/*`) + net48 transport edge (`Features/PilotIdentity/*`, not link-compiled); six named ATs green under `dotnet test`. No account creation. status: proposed — needs writer≠verifier review + live dedicated-server proof (deferred to final gate). |
| account-identity-pilot-tracer1-evidence.md | IAP-003 Tracer 1 executed evidence (t_0f1a9160): the first-bind account+credential foundation — HMAC-only disclosure-aware allowlist, server-minted 128-bit `AccountId`/`CredentialBindingId`, atomic account+credential first bind, framed CRC lifecycle journal with torn-tail quarantine, boot-rehydrated indexed lookup, active/previous domain-separated HMAC key ring with version census + safe retirement gate + multi-hop lazy re-key, and explicit no-name/no-auto-merge behavior. Engine-free CLEAN-side core (`Domain/Accounts/*`, `Adapters/Identity/LookupKeyRing.cs`, `Persistence/Accounts/*`, `Application/Accounts/*`); 25 named ATs green under `dotnet test` (823/823 suite), net48 mod build 0 warnings. No character mint / receipt migration (Tracer 2/3). status: proposed — needs writer≠verifier review + live dedicated-server proof (final gate). |
| account-identity-pilot-tracer2-evidence.md | IAP-005 Tracer 2 executed evidence (t_afc5e5c9): minted characters + single-session admission on the merged Tracer-1 foundation — server-observed nonzero `s_playerID` mapped through a domain-separated profile HMAC to a server-minted, account-scoped opaque `CharacterId`; one pending/active admission lease reserved BEFORE character mint; race-safe/idempotent/matching-session-released/stale-disconnect-safe ephemeral leases; cross-account isolation; previous-key profile re-key in place (same `CharacterId`); and the vanilla `s_creator` creator-evidence bridge to the internal character. Engine-free CLEAN-side core (`Adapters/Identity/PilotProfileSubject.cs`, `Application/Accounts/{AccountAdmissionIndex,PilotCharacterAdmissionService}.cs`, character projections/profile index on `Persistence/Accounts/PilotAccountStore.cs`); 11 named ATs + 5 guards green under `dotnet test` (859/859 suite), net48 mod build 0 warnings. No receipt migration (Tracer 3) / operator-privacy (Tracer 4). status: proposed — needs writer≠verifier review + live dedicated-server proof (final gate). |
| account-identity-pilot-operator-evidence.md | IAP-009 operator-control foundation executed evidence (t_32cdc8ea): the CONTROL subset of Tracer 4 — OS-scoped local allowlist bootstrap (owner-only path, no-echo stdin, allowlist-only verb scope), live-admin inspect (subject-free summary), disable (fence→drain→atomic commit→deterministic session close, durable across reboot), per-account mutation fence + drain barrier (failed drain leaves a recoverable Active state), and delete-drain that revokes linked credential + allowlist so a stale allowlist cannot recreate the account. No second admin path is exposed to gameplay payloads. Engine-free CLEAN-side core (`Application/Accounts/*`, `Features/PilotIdentity/LocalAllowlistBootstrap.cs`); 7 named ATs (AT-AIP-ADMIN-INSPECT/-DISABLE/-DISABLE-CLOSES-SESSION/-LOCAL-BOOTSTRAP-SCOPE/-NONADMIN-REJECT/-MUTATION-FENCE/-DELETE-DRAIN-BARRIER) green under `dotnet test` (851/851 suite), net48 mod build 0 warnings. Privacy/purge lifecycle + live joined-client proof deferred (IAP-010/IAP-011). status: proposed — needs writer≠verifier review (IAP-011) + dedicated-server proof (IAP-010). |
| account-identity-pilot-operator-qa-evidence.md | IAP-010 executed QA evidence (qa-playtest, t_58ba450f): the IAP-009 operator controls run on the dedicated Niflheim QA server host against REAL OS resources, using only the `ForTheWort_QA` QA-only identity (not a regular character, not Pololol). A real-OS host (`qa-operator-harness/`) link-compiles the SAME shipped IAP-009 cores the test suite compiles and binds them to a real on-disk account journal, real `stat(2)` ownership bits, real no-echo stdin, a real concurrent in-flight mutation, and a GENUINE second-process restart. 23/23 operator checks PASS across two real OS processes: protected allowlist bootstrap (real fail-closed), first-join discovery, live-admin inspect (subject-free), disable draining an in-flight mutation + deterministic session close, failed-drain `DrainTimeout` recovery, delete-drain with allowlist revocation, post-disable `NotAllowlisted` rejection, and restart durability/session-clearing. Persisted journal proven raw-subject-free by recursive base64 decode; HMAC key `0600` then `shred`-ed. Verified vs last-mile split explicit: the Valheim graphical-client join/kick stays with Daniel + IAP-011. status: proposed — needs writer≠verifier review (IAP-011). |
| account-identity-pilot-tracer4-evidence.md | IAP-012 Tracer 4 executed evidence (t_38e47d2f): the privacy foundation over the merged account foundation (PR #330) — player-safe cataloged export (no raw subject/HMAC/key-version/unrelated-account/secret leak), configurable positive retention (shipped 14/30 defaults, zero/negative rejected, decrease-immediate / increase-requires-new-notice-ack gate), scoped/reasoned/expiring incident holds (global/expiryless rejected, auto-resume on expiry), pilot closure with a derived purge deadline that rejects later enrollment, and mandatory pre-use artifact cataloging with fail-closed admission on an uncataloged world fixture + evidence-digest-gated purge across every artifact class. Engine-free CLEAN-side core (`Domain/Accounts/PilotRetentionPolicy.cs`, `Application/Accounts/PilotPrivacyService.cs`, extended `Persistence/Accounts/PilotAccountStore.cs`); 8 named ATs green under `dotnet test` (854/854 suite), 0 warnings. Delete/purge fence, admin inspect/disable, whole-fixture reset, backup/retention purge, breach runbook, and dedicated-server proof remain outstanding Tracer-4/final-gate work. status: proposed — needs writer≠verifier review + live dedicated-server proof (final gate). |
| homestead-emergent-party-dynamics.md | BRAINSTORM (architect, card t_b7ea5c03, 2026-07-16): explores whether useful co-play emerges from existing Homestead primitives (Bond/Attunement relationships, per-Stone permissions, Responsibility Range, Settlement beneficiary policy) BEFORE any formal Party aggregate. Grounds every claim against the accepted data-model/spec. Key findings: "natural party" = the shared-Stone cohort as a DERIVED read-model (the Stone is the party object we already have); permission must resolve by INTERSECTION per-(character, Stone, action), never union (the escalation trap); NO group credit — character AP/BP is never pooled or read by a group; membership keys on CharacterId respecting account sibling exclusivity; context is target-driven not an ambient active-group mode. Scenario matrix shows 6 of 7 co-play scenarios need NO Party aggregate; the ONE thing that genuinely needs new state is a consent-to-PERSON primitive (transient invite/accept) — the sole justification for the Party horizon. Recommends starting with derived-view (Option 1) + explicit named revocable cross-Stone grants, adding only the bounded records that prove underivable; do NOT build a Party god-object. Carries T1–T8 exploit sweep and 5 reserved Daniel decision points. Grounded input the Party/Guild wayfinder (t_33147639) consumes; no implementation, no formal Guild design. |
| stone-content-authoring-workbench.md | POC (card t_e2de37e4, 2026-07-16): narrow production-intent vertical slice proving a declarative JSON asset + deterministic C# generator can round-trip the current 20-node Homestead Stone roster WITHOUT replacing the hand-written C# catalogs. net8 `StoneContent.Workbench.Core` deep module (I/O-free load/validate/classify/generate/check) + thin CLI (`validate`/`generate`/`check`) and a loopback-only local browser workbench (`StoneContent.Workbench.Web`, UI card t_e4d16b1c) that reuses the core for all authoritative behavior — Cooking-node editing slice, exact JSON diff, narrow generated-C# diff, manual version pins, atomic scratch export with stale-write detection; 1440×900 acceptance screenshots under `tools/stone-content-workbench/docs/evidence/`. Behavioral parity: three current-`main` axes (contentRegistry/foundationalCatalog/facetPalette) PASS against the real production catalogs via a normalized snapshot adapter; Tree tuning is a HELD-branch reference (T012 `wt/t_c7313d0f`), reported current-main NOT APPLICABLE — no four-axis parity claim. Deterministic double-generation is byte-identical; generated scratch output compiles under net8 (real harness). Version pins never auto-bump. Tool under `tools/stone-content-workbench/`. **status: idea** — POC only; production migration gated by the promotion gate. |
| party-guild-wayfinder.md | WAYFINDER (architect, card t_33147639, 2026-07-16, status: idea; CORRECTED 2026-07-21 for owner decisions t_b7ea5c03/t_33147639): charts the way from the Homestead substrate + emergent-party findings to a buildable formal Party (ephemeral) and later Guild (durable) system, as a map of investigation tickets + settled owner decisions. Owner-settled Party model (supersedes the earlier derived-cohort premise): a **formal Party object is required** — Homestead ownership/Attunement and later Friends are discovery/suggestion inputs, not Party identity; Party is **ephemeral** (explicit logout drops membership; unclean disconnect retained only for a fixed reconnect-grace timeout); max size **3 characters (configurable)**, dissolves at one member; all members are **peers** (creator/inviter is event provenance, never an enduring leader); forced removal needs a **majority excluding the target** (2 votes at max 3; a two-member Party cannot force-remove — either leaves, dissolving it). Preserved safety invariants: CharacterId-scoped membership, no pooled character AP/BP, per-action permission checks with no union-of-privilege escalation, target-scoped Stone authority. Defines the Party↔Guild boundary as a hard invariant (§4: Party = ephemeral lifecycle over a membership set; Guild = durable identity that OWNS memberships and DECLARES Stone affiliations) — Guild is explicitly NOT \"Party but persistent.\" Ubiquitous-language glossary; investigation tickets ordered by dependency/risk with gate wiring; architecture shapes (thin formal Party recommended first; bounded Guild aggregate later; unified mode-flag aggregate documented ONLY to be rejected as the forbidden anti-pattern); formal threat model P1–P8 extending emergent T1–T8; provisional vertical slice (Party first, Guild waits for Community/Settlement Stones). Discord integration shelved to a future horizon (must not shape the initial Party model). No cards, no specs, no code. Grounded input the synthesis card (t_213d4aaa) consumes. |

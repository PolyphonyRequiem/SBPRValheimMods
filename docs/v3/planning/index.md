# index — docs/v3/planning

| file | purpose |
|------|---------|
| README.md | Human orientation for v3 (Swamp tier) planning |
| index.md | This manifest |
| config-bakedown-impl-spec.md | PROPOSED (architect spec-pass, card t_f87361cf, DEFERRED-then-unblocked post-Playtest-#5): classifies all **50** live `Config.Bind` knobs (verified on main @ 7769812; the card's "~54" predates the #239 Ring→Halo swap) into 3 buckets — (A) dev diagnostics to remove/default-OFF, (B) tune-then-bake provisionals to freeze ONLY on Daniel's lock, (C) keep-live player/server config — and proposes the root-cause fix for the stale-default-drift UX wart (a bumped code default silently masked by an old `.cfg` value; verified victims `DetectRadius` 30→50 #234, `MinimapBlipStyle` Dots→Trophy #238). 🔴 Corrects 3 card premises: (1) **`SpecCheck.cs` has NO config manifest** to sync — it's a recipe/piece/icon/attack watchdog only, so the card's "sync SpecCheck" acceptance is moot (optional net-new `ConfigCheck.cs` = separate card); (2) **`SBPR_BannerDiagnostic` must stay LIVE** — the card lists it in Bucket A yet it binds inside the hard-excluded `[CairnBanner]` block and is the live instrument of windsock attempt #6; (3) **Bucket B is nearly empty now** — Halo* (#239) just shipped and the compass feel's in-game accept is still pending, so almost nothing is locked-and-bakeable. Sharpens the fix beyond migration: the drift victims are *mis-classified* (Daniel-via-PR knobs masquerading as live config) → baking them makes drift impossible by construction. 6 gates for Daniel (which knobs bake, compass-feel lock, DebugCairnDamage flip-vs-remove, kept-live migration policy, optional ConfigCheck, windsock confirm). AT-CFG-* acceptance; build 0/0; docs-lint green. Clean-side (ADR-0001), SpecCheck +0. **status: proposed** — Daniel gates the classification + locked figures, then an engineer-ui impl card executes the bake-down + doc-row sync. |
| twisted-portal-impl-spec.md | Build-ready spec for the **Twisted Portal** — a distinct portal class that teleports even where vanilla portals are blocked (`NoPortals`), addressed by player-assigned RUNE NAMES (`sbpr_rune_name` ZDO slot, off `s_tag`). **Cost model = FOOD-AS-FUEL (no key trinket)**, RECONCILED 2026-06-24 (card t_c15411b2) to the locked design `twisted-portal-food-charge.md` (PR #270/#271): Portal Energy `PE(slot)=remaining_minutes×tier(food)` summed over belly slots, a teleport spends PE as food-time scaled by distance (distance both costs provisioning AND lands you depleted), and Bukeperries (vanilla `Pukeberries`) are a burnable emergency reserve spent only on the shortfall (30 m/berry, 10=300 m ceiling; a berry-jump arrives food-empty AND `SE_Puke` Feeling Sick). The former `SBPR_TwistedKey` durability-battery economy + its 3 Harmony patches are REMOVED — **the cost model is patch-free** (PE read on demand from `Player.GetFoods()` `:17598`; tier reads the food's BASE stat budget off `m_shared`, NOT the decayed live stat). Reimplements teleport via `Player.TeleportTo` (`:20771`, omitting the NoPortals gate `:123008`) rather than inheriting `TeleportWorld`. Catches the multiplayer 300 m-sync fork (client holds portal ZDOs only within ~64–128 m on a dedicated server) and routes travel through server-side rune-name pairing (Model A). **Q2 resolved → food-as-fuel; Q1=coexist + Q3=Model A (informational through-terrain overlay) LOCKED** (the old 3-decision Daniel gate is retired). SpecCheck **+1 row** (portal piece; the key row is removed). Decomposes into already-created impl cards C1 (t_2b388cd5 portal core, engineer-systems), C2 (t_6e992a30 food-as-fuel cost, engineer-systems), C3 (t_e732bd8b through-terrain overlay, engineer-ui) — they auto-promote when this reconciliation merges. **status: proposed** — Daniel gates the docs-only reconciliation PR (original card t_f9cab392). |
| trail-lights-impl-spec.md | Architect+spec for the v3 trail-light family (card t_117bc232): two distinct ETERNAL Spade-placed pieces — a tall far-reaching Beacon (light 1.5/12, kept from nomap §3) and a small Surtling-Ember Lamp — gated by Surtling core (+ Iron on the Beacon per the Q3 lean). Records the decomposition rationale (v1's Path Lamp already fills the fuelled niche), corrects the card's stale `ConfigureCosmeticFire` reference to the current `Assets.GraftTorchFire` eternal-flame pattern (no Fireplace → no add-fuel hover by construction), and reserves the v5 lighthouse-promotion hook. SpecCheck +2 build-piece rows. **status: proposed** — needs Daniel's confirm on 4 open questions before an impl card is cut. |
| sunstone-lens-impl-spec.md | Build-ready spec for the Sunstone Lens — a Trinket-slot solar-charged monster-detection accessory (card t_2fd7bc7f). Durability-as-energy (recharges in clear daylight, drains at a fixed rate while worn), reproduces rune-of-detection's behavior from vanilla primitives (Character.GetAllCharacters faction/tamed filter), renders via a NoMap-safe HUD overlay (Iron Compass doctrine). SpecCheck +1 row (Lens recipe; the Sunstone material is loot-sourced, no recipe row — provisional craft REMOVED per card t_c27f985e). Sunstone loot economy shipped (card t_0445f590 / PR #183). |
| sunstone-minimap-handoff-impl-spec.md | AS-BUILT spec for the Sunstone Lens → minimap detection handoff (card t_91e86951, graduated from design PR #214). Daniel gated 3 knobs (MinimapHandoffMode=DiscWhenBound, blips=dots+aggro-tint, the universal any-minimap-present rule) then directed "build the whole thing." Ships: the engine-free `LensHandoffDecision` truth table (CI-gated, link-compiled into the test project), the `SunstoneProjection`/`ThreatBlip` lift (one Character→tint/trophy/stars derivation, three surfaces — AT-LENS-DISC-NODRIFT), the Cartography `IThreatMarkerProvider` seam for the SBPR carry-disc (nomap-ON, mirrors WorldPins), a custom `SunstoneMinimapThreatLayer` overlay on the vanilla corner minimap (nomap-OFF — `AddPin` grounded OUT, vanilla stomps pin colour every refresh), two live Config enums, the #209 dead-Update-pump guard (ring hides via `_content`, never the host), and the AT-LENS-DISC-* tests. Render-only: SpecCheck +0 rows, no new prefab (ADR-0006 N/A), clean-side (ADR-0001). **status: implemented** — build 0/0, 186/186 unit tests; logs-green ≠ playable (in-game ATs are the qa-playtest card + Daniel gate). |
| sunstone-loot-economy-impl-spec.md | Build-as-shipped spec for the Sunstone dual-source loot economy (card t_0445f590): a Sunstone DropData injected into the vanilla `TreasureChest_swamp` Container DropTable (PRIMARY, ~15% per chest — solved from the weight-based, sample-without-replacement vanilla algorithm, NOT a raw percent) + a Sunstone Drop injected into the `Draugr_Elite` CharacterDrop (SECONDARY, 5% flat across stars). EXCLUDES `TreasureChest_sunkencrypt`. Additive list-append (ADR-0006, no cloning); ZNetScene-phase, idempotent, after SunstoneLens registration. Records the 15%-per-chest→m_weight=0.584 solve (reproducible: `scripts/solve_sunstone_chest_weight.py`) and the vanilla-consistent already-populated-chest limitation. SpecCheck +0 rows (loot tables aren't manifested); the provisional-recipe REMOVE is the architect's card t_c27f985e. **status: implemented** — build 0/0; logs-green ≠ playable (qa-playtest card t_0aef1243 + Daniel gate). |
| iron-compass-impl-spec.md | Architect+spec for the v3 Iron Compass (card t_d35405e3): a worn, Iron-gated TRINKET accessory whose HUD overlay grants cardinal orientation WITHOUT touching the local map — the earned payoff of the no-map pillar (v1/v2 ship the map with no north indicator by design; this is the separate earned tool). Additive ItemDrop (`SBPR_IronCompass`, `m_itemType=Trinket` enum 24) + a `[HarmonyPatch(Hud,"Awake")]` postfix that mounts an `SBPR_CompassHud` UGUI overlay under `Hud.instance.m_rootObject`; the needle is driven from `GameCamera.instance.transform.eulerAngles.y` with lerp lag, pitch mapped to a ~45° UI tilt. **Corrects the design note's `HaveItem` carry-gate to the `GetEquippedItems()` Trinket-slot equip-gate the Cartographer's Kit proves in-repo.** Flags the card's misread of `requirements.md:696` (Iron Compass custom mesh is DEFERRED to v0.2+, not mandated) as an open question for Daniel. One Harmony patch (the `Hud.Awake` postfix — PatchCheck applies, NOT patch-free), no game-state patches, no map mutation. SpecCheck +1 item recipe row. **status: IMPLEMENTED** (card t_ee61472f, 2026-06-17 — Daniel's Q1–Q4 answered: recipe `Iron ×4 + Ooze ×2 + Red Pigment ×1`; sprite HUD, world mesh deferred to v0.2+; lerp lag Config.Bind-tunable; anchor a Config enum default `TopCenter`, NoMap-safe, extensible to the carry-disc + Eye-of-Odin minimap). Build 0/0; logs-green ≠ playable (Daniel gates the playtest). |
| iron-compass-minimap-ring-impl-spec.md | Buildable spec graduating the GATED iron-compass-minimap-ring DESIGN (card t_ed803a83, from t_85a46f42 / PR #226). When the Iron Compass is worn AND an SBPR map surface (carry-disc OR full-map modal) is showing, draw a compass-gated north ring on that surface — iron-bezel recolor (per-frame `_bezel.color` tint off `IsWearingCompass`, reverts to bronze unworn) + an N-glyph **chevron-sibling** on the rotating `_mapContainer` (the `_playerMarker` idiom, counter-rotated upright) — else the unchanged TopCenter HUD needle (it hides when the ring is up). **Two milestones:** M1 = iron bezel + N-ring (high value, low risk); M2 = opt-in `CompassAutoNorthUp` north-up lock (default OFF, §6). The N marker is DELIBERATELY NOT routed through the shipped `IThreatMarkerProvider` (wrong shape: world-positioned/per-rebuild/disc-only — the compass N is screen-bearing/per-frame/both-surfaces, NO new pull-seam). Engine-free `CompassNorthGate` truth table (CI-gated, AT-COMPASS-GATE) + `CompassDiscMode` enum; the §5 "north is compass-gated" rule reconciles the Sunstone twin (AT-DISC-NORTH-GATED). Carries the §4.3 supersession re-wording of iron-compass-impl-spec + cartography-impl-spec (spec+doc together). Render-only: SpecCheck +0, no new prefab (ADR-0006 N/A), clean-side (ADR-0001). Grounded against `main` @ cdd5cc2. **status: proposed** — Daniel gates the impl-spec at doc review + the in-game ATs; engineer-ui builds M1. |
| sunstone-lens-corona-impl-spec.md | Buildable spec graduating the `/bug` report on card t_2d500d45 (Daniel, 2026-06-22): the Sunstone Lens "solar ring" is a flat **screen-space** annulus (`SunstoneLensHudOverlay._emptyRing` :164, `RingSprite()` :455, `CSolarRing` :112 = RGBA 0.98/0.78/0.36/0.18), never the 3D slowly-pulsing "sun corona" disc Daniel pictured. REPLACE the flat ring with a **world-space corona disc** drawn into `SunstoneWorldRing`'s scene root (the substrate the fixed-distance trophy halo orbits, t_10bacccf/#248), breathing on a slow alpha pulse. **The design gate DISSOLVED** (Daniel declined to pre-approve — "why am I needed"): every visual knob is resolved by a **reversible live-config default** — orientation→`GroundPlane` ("sun on the floor", live-flippable to camera-facing), art→filled radial glow, pulse→~0.25 Hz (α 0.10↔0.28 around the 0.18 baseline), replace-not-stack — so **no pre-build look-lock is required**; Daniel's in-game eye on a GPU client is the accept AFTER ship, not a gate before it. The pulse envelope is the engine-free, CI-gated `SunstoneCoronaPulse` (link-compiled into the test project, the `SunstoneHaloGeometry` precedent — AT-CORONA-PULSE-MATH). Supersedes the flat-pulse no-minimap half of t_acaa0190 (`sunstone-lens-aura.md`); its minimap-rim re-homing half is a SEPARATE **parked** follow-up (NOT built here, NOT blocked on). Render-only: SpecCheck +0, no new prefab (ADR-0006 additive), clean-side (ADR-0001). Grounded @ `main` 21f7a92 (#248). **status: proposed** — Daniel gates the impl-spec at doc review + the in-game AT-CORONA-* ; engineer-ui builds it. |
| sunstone-lens-aura-impl-spec.md | Buildable spec graduating the SURVIVING half of Daniel's aura idea (card t_acaa0190 → built by t_7416e5b9). The flat-ring half died when the world-space corona (t_2d500d45/#254) REPLACED the `_emptyRing` it was going to animate; the corona-spec §6 PARKED the remaining "aura on a minimap surface" half conditional on Daniel still wanting it. On 2026-06-22 (Discord `ticket-diegetic-halo-render`) Daniel confirmed: "implement 241, apply it to the new design." This RE-HOMES that half onto the new design: INSTEAD of a separate gold annulus on the minimap rim / carry-disc bezel (the parked plan, which collided with the Iron Compass `_bezel.color` tint), **DECOUPLE the world-space corona from the threat trophies** — when a minimap takes the threat feed (the shipped Sunstone→minimap handoff hides the ring's trophies), the corona KEEPS breathing on the ground so the lens always reads "live" on every surface; only the trophies hand off. One new output `CoronaContentVisible` on the engine-free, CI-gated `LensHandoffDecision` truth table (`coronaContentVisible = ringContentVisible || CoronaPersistsOnMinimap`, +8 AT-AURA-PERSIST-MATH assertions) + one live knob `CoronaPersistsOnMinimap` (default ON, reversible). The render reuses the shipped `ShowRootWithoutTrophies()` + `RenderCorona()` depleted-hint mechanism at full live envelope; #209 host-pump guard intact. Render-only: SpecCheck +0, **no new prefab/surface** (ADR-0006 N/A — reuses `SunstoneCoronaDisc` + `SunstoneWorldRing` root), clean-side (ADR-0001). Grounded @ `main` a6e7b80. **status: proposed** — build 0/0, 292/292 unit tests; Daniel gates the impl-spec at doc review + the in-game AT-AURA-LIVE-* ; logs-green ≠ playable. |

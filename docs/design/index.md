# index — docs/design

| file | purpose |
|------|---------|
| README.md | Human orientation for the design folder |
| index.md | This manifest |
| architecture-review.md | PROPOSED (architect, card t_792c699b, 2026-06-17): full architecture review of SBPR.Trailborne. Names the 4 domain models the mod needs (A: ZDO-backed component / the *Tag family; B: recipe-definition single-source that retires the Registrar↔SpecCheck drift; C: cartography provider/surface; D: trinket/charged-accessory) + the engine-free domain-core SEAM that makes them testable (mirrors the substrate + the in-repo BoundedMapMath/SignHoverHintText proof). Maps each model to the churn it stabilizes; checks fit vs planned v3/v4/v5 + Eye-of-Odin. Phased reversible refactor P0–P6, each 0/0 + Daniel-gated; P0–P2 executed under the card. Shared input for the CLEANUP batch (cards 1/3, 3/3) |
| trailborne-vision.md | North star: the Explorer role and what success feels like |
| trailside-camp.md | LIVING design (Daniel, 2026-06-24 #design thread): the **Trailside Camp** — a portable triad (Bear Hide Tent + special bedroll + covered small camp fire) that together satisfy Valheim's vanilla SLEEP prerequisite out on the trail. 🟢 GROUNDED hard against the decomp: `Bed.Interact` runs a 5-gate chain (time/no-enemies/`CheckExposure` underRoof+0.8cover/`CheckFire` Heat-area/`CheckWet`); the open tent canopy passes `underRoof` (free: keeps player dry, keeps fire lit in rain, passes the roof half) but tops out ~0.47 cover, FAILING only the 0.8 `CheckExposure` clause — which the **special bedroll**'s prefab-gated `Bed.CheckExposure` relax exists to drop (keeping underRoof so no open-sky sleep). 🟢 DECIDED 2026-06-24: tier=**Black Forest** (bear `Bjorn`=BF creature); sleep=**skip-night-only, NO spawn set** (runs `AttachStart isBed:true`→`SkipToMorning` without `SetCustomSpawnPoint`); **comfort**=free vanilla `SE_Rested` on wake; **Inspiration**=the beautification doc's already-locked **Inspired** Appeal buff (skill-gain, granted via its "non-cairn comfort" hook — camp consumes it, doesn't fork it). Tent placeholder = vanilla `TraderTent` (hide texture, 8.0×4.9×6.9 m, legs kept, VISUAL-ONLY). Also the `trailside-beautification.md` "tents" beautifier (that doc owns the aura; this owns the sleep mechanic). 5 OPEN knobs (Spade-vs-Hammer, storm-proofing, 3-pieces-vs-combo, underRoof-keep, bedroll-Inspired-reconcile). Net-new, NOT in requirements.md v1 |
| trailside-beautification.md | LIVING design (Daniel, 2026-06-24 #design thread): the Trailside Beautification system — trail furniture (Painted Signs, Path Lamps, Traveller's Cache, terrain paint, tents) aggregates into tiered area buffs across three categories (**Convenience / Safety / Appeal**) via a **cascade quality-gate** (a piece's % feeds its own tier AND every tier below; highest meter ≥100% wins — so T1 spam can never buy a T3 buff). Field property of **overlapping coverage zones** (~32 m, 3× vanilla's grounded 10 m comfort) — NO "waystation" entity. 🟢 The three buffs are LOCKED + decomp-grounded: **Convenience** = stamina-consume reduction (no weight/speed); **Safety** = *Relaxed*, slows food digestion so provisions last longer (+ secondary base anti-spawn); **Appeal** = *Inspired*, an accumulating timed skill-gain buff also granted by non-cairn comfort. 🔑 Cairn comfort/Rested is a SEPARATE shipped system (only the cairn *piece's* Appeal % is an open seam). Most substrate already SHIPS (Lamps/Signs/paint); new work = the cascade engine + the 3 buffs (2 stat-fields, 1 small UpdateFood patch) + Lamp/Sign upgrade tiers. Starts Meadows, scatters upward. 8 OPEN knobs incl. a real Meadows-vs-Black-Forest cache-tier conflict. Cross-links trailside-camp.md (tent = shared piece). Net-new pillar, NOT in requirements.md v1 |
| design-pillars.md | The load-bearing design constraints, non-negotiable |
| nomap.md | Investigation: how Valheim tracks the map + no-map patch-surface cross-reference |
| map-provider-model.md | LIVING design (Daniel 2026-06-15 revision): the map provider model — M-key behavior, two map types (personal global map + local-map ARTIFACTS), equipped-local-map "provider" binding, Cartographer's-tools + Surveyor's-Table bidirectional global↔local sync, nomap-on/off split, Eye of Odin (Mistlands global-map unlock). Serialization grounded vs decomp (carried customData ≠ networked → artifact model safe). Supersedes the M/E-open + nerf portions of cartography-impl-spec §2G; iterating on §9 open questions before it graduates to specs |
| pin-sharing.md | Investigation: multiplayer pin-sharing surface under server gating |
| constitution.md | Governing principles for spec-driven v2 (ADR-0005 Option C) — Spec Kit vocabulary + load-bearing invariants |
| cartography-v2.md | DRAFT design: Black Forest cartography — Map Station + Local Maps + Cartographer's Kit (grounded on vanilla MapTable/Minimap; open questions pending Daniel) |
| forge-masters-trinket.md | PROPOSED (architect, card t_f9ba4bdf, 2026-06-18): build-ready spec for the Forge Master's Trinket — a STANDALONE-mod Trinket whose power, fired when the vanilla adrenaline bar caps (~80), repairs +5 durability on EQUIPPED gear (scope locked 2026-06-17) instead of a combat burst. 🔴 Grounding inverts the card's premise: adrenaline is a LIVE vanilla Ashlands trinket mechanic (m_maxAdrenaline cap + m_fullAdrenalineSE cap-fire hook on Player.AddAdrenaline :19490-19514, ItemType.Trinket=24), NOT a custom meter — build is ~95% vanilla (one StatusEffect SE_ForgeRepair + one additive item, Sunstone Lens template). Equipped-set = vanilla UpdateEquipment 8 slots via public GetEquippedItems(). Carries: repo/project structure proposal (Option A separate repo vs Option B in-monorepo standalone DLL — recommends B, leaf-not-substrate vs the WorldZones hybrid model); provisional namespace SBPR.ForgeMaster; AT-FORGE-1..11; and the single-trinket-slot tension vs the Sunstone Lens. For Daniel's ratification before scaffold+impl cards. Home=standalone locked 2026-06-18 |
| marker-signs-worldpin.md | LOCKED design: Marker Signs + the SBPR WorldPin substrate — durable ZDO-anchored map pins, Shift+E pin/unpin, destroy-safe offline via derive-by-scan reconcile (the pin model the v2 cartography tier consumes) |
| swamp-detection-item.md | IDEA: Swamp-tier solar-charged monster-detection accessory — charges durability in sunlight, drains while equipped, reveals nearby hostiles. Theme + sourcing DECIDED 2026-06-13 (Sunstone / Iceland-spar, dual-source: swamp surface chests + rare Draugr Elite); mechanics still proposed |
| sunstone-lens-minimap-handoff.md | PROPOSED (architect, card t_3129842a, 2026-06-20): Daniel's idea — when a local-map minimap disc is available, move the Sunstone Lens hostile detection onto the disc instead of the camera-relative trophy-ring HUD. Grounds the trigger (`CartographyViewer.IsMinimapBound` = nomap-ON + local map bound + imprinted — precisely the config the old "no surface" rationale was true for), the lowest-coupling seam (a new Cartography transient-threat-marker provider mirroring `WorldPins.CollectInDiscPins`; the FIRST Sunstone→Cartography dependency, no zero-API option), and the shared `(Character → pos/tint/trophy/pips)` projection both renderers consume (zero-drift). 🔴 Geometry quantified: detect 70 m vs disc span 125 m (~62.5 m radius) → detection now EXCEEDS the disc; near threats are on-disc, the 62.5–70 m band rim-clamps into the bezel (argues dots+tint over trophy art, and closes the old disjoint-from-compass-N margin — flagged for in-game eye). 🔴 Converts the load-bearing replace-vs-supplement knob from a build-blocking fork into a live `MinimapHandoffMode` Config enum (banner-windsock), default `DiscWhenBound`. Locks the #209 dead-Update-pump pitfall (hide the ring's `_content`, NEVER the host) + AT-LENS-RING-CAMREL survival (disc rotates to camera-yaw, not north). 3 OPEN knobs for Daniel. CLEAN-SIDE, SpecCheck +0. Gate → `sunstone-minimap-handoff-impl-spec.md` |
| iron-compass-minimap-ring.md | PROPOSED (architect, card t_85a46f42, 2026-06-21): the TWIN of the Sunstone handoff, mirror-imaged on north. When the Iron Compass is worn AND an SBPR map surface (carry-disc OR full-map modal) is showing → the surface gains a compass-gated north indicator (IRON bezel + N + ticks, reverts to bronze when unworn); else the current TopCenter HUD needle. 🔴 Consciously SUPERSEDES two locked theses: (§4) the iron-compass impl-spec's "the compass NEVER adds a north arrow to any map" → "never UNGATED; the compass-gated ring is the sanctioned exception"; (§6) the `MapRotationSign` "no north-up alternative" lock → an OPT-IN auto-north-orient config (default OFF). 🔴 RE-GROUNDED against what SHIPPED (main @ b618aa8): the already-merged `IThreatMarkerProvider` (t_91e86951) is world-positioned/per-rebuild/disc-only — the WRONG shape for a screen-bearing N marker, so it is deliberately NOT reused; the N marker is a player-chevron sibling on the rotating `_mapContainer`, needing NO new pull-seam. Resolved with the twin by ONE rule: the surface shows north IFF the compass is worn (AT-DISC-NORTH-GATED). Daniel's 4 answers folded (HUD hides / N+ticks on gated iron bezel / opt-in auto-north default OFF / disc+modal scope). Locks AT-COMPASS-DISC-PUMP (#208/#209). 1 OPEN knob (nomap-OFF vanilla minimap). CLEAN-SIDE, SpecCheck +0. Gate → `iron-compass-minimap-ring-impl-spec.md` |
| sunstone-lens-aura.md | PROPOSED (architect, card t_acaa0190, 2026-06-21): Daniel's /bug idea — while the Sunstone Lens is worn, a faint golden aura PULSES "around the outer rim of the minimap (or as the art for the no-minimap ring)." 🔑 REFRAME: the gold ring is NOT net-new — SBPR already draws it as the lens' empty-state affordance (`SunstoneLensHudOverlay._emptyRing` :164, colour `CSolarRing` :101 = RGBA 0.98/0.78/0.36/0.18, sprite `RingSprite()` :579). This is EXTEND+ANIMATE: (1) make the existing ring pulse (net-new — zero breathing-alpha exists in-tree), (2) re-home that pulsing aura onto the minimap rim when a minimap owns detection. 🟢 CENTRAL INSIGHT: Daniel's "(or as the…)" maps exactly onto the ALREADY-SHIPPED Sunstone→minimap handoff (t_91e86951) — the handoff hides `_content` (and `_emptyRing` with it, :315) when a minimap owns detection, so the aura is simply the empty-ring re-homed onto whichever surface `LensRenderPlan` already points at: ONE concept, no new arbitration/gate/art. Corrects 3 bug-card grounding errors (MapSurface.cs is Features/Cartography/ not /Sunstone/; the disc-bezel setter routes through `MapViewer.SetCompassNorth` → BOTH `_disc` AND `_modal`, two surfaces; vanilla-minimap rim mounts north-up under m_pinRootSmall, no counter-rotate) and surfaces 1 architecture finding the card missed: on the carry-disc the bezel is ALREADY a per-frame colour consumer for the Iron Compass iron/bronze tint (:1163) — a lens aura on the SAME `_bezel.color` channel COLLIDES (resolution: separate concentric gold Image, Knob 1c). 2 knobs resolved by grounding (surfaces follow detection; unify not split), 3 are eyeball calls (pulse style/rate/depth/intensity) → Daniel look-locks via the interactive HTML mock (`~/sunstone-aura-mock/index.html`, Hz + trough/peak-α sliders, reads off paste-ready impl constants; he's colourblind so gold is named by RGBA not hue). CLEAN-SIDE, SpecCheck +0, no new art. Grounded @ aa03e21. Gate → `sunstone-lens-aura-impl-spec.md` |
| pocket-portal.md | PROPOSED design: Portal Seed → Ancient Portal (Black Forest v2) — single 25 kg field item, Hammer-placed no-bench, plants and grows over 15 s into a horizontal overhead root-portal, otherwise a regular portal (keeps ore ban), collapses back to a replantable seed when broken |
| twisted-portal-food-charge.md | ACCEPTED (Daniel-locked 2026-06-24, tuning baseline): the v3 Twisted Portal cost model — NO key trinket; teleport range gated by belly food. Portal Energy (PE) = remaining-food-minutes × stat-derived tier, summed across food slots; a jump spends PE as food-time so distance costs provisioning AND lands you depleted (built-in arrival penalty). Tier = round(clamp(total_stats/30, 1, 5) × 2)/2, snapped to 0.5 rungs [1.0,5.0] — the STAT FALLBACK is the primary rule, so vanilla + modded/unaccounted foods slot in with zero hand-authoring (optional override registry may pin individual foods later). Rewards progression (better food = farther) over cooking-effort. Feasts run on a separate normalized ~28 m RANGE clock (their 50 m BUFF timer untouched) → land slightly under personal crafted meals for travel (best feast PE 140 < top personal 150). BUKEPERRIES (vanilla Pukeberries, Greydwarf/Fuling shaman drops) = burnable EMERGENCY RESERVE PE: spent ONLY when belly food can't cover a jump, 30 m/berry (10 berries = the 300 m portal ceiling); a berry-burning jump arrives food-empty AND Feeling Sick (vanilla SE_Puke), advertised ONLY by a Greydwarf-portal-magick lore breadcrumb (no UI). All food numbers pulled from the wiki corpus, not estimated. SUPERSEDES the trinket-key/durability charge economy in nomap.md §7 (:146,:154-158) + the food-charged-key premise in docs/v3/planning/twisted-portal-impl-spec.md (resolves its open decision #2 'charge economy'); the old Bukeperry purge-accelerator is RE-HOMED as the reserve tank, not dropped; portal class / rune-name pairing / NoPortals bypass / through-terrain overlay unaffected. Open tuning knobs: eitr weighting, feast cap (28 m), /30 divisor + [1,5] clamp, override registry, Bukeperry reach (30 m/berry), the SE_Puke arrival debuff, shaman stockpile rate. |
| travellers-cache.md | PROPOSED design + architecture: Traveller's Cache — a placeable trailside chest with ONE shared public inventory plus a per-player private compartment (keyed on Player.GetPlayerID() long), persisted as a compressed ZDOVars.s_data blob via the in-tree Surveyor's Table pattern, additive prefab (ADR-0006), WearNTear.m_onDestroyed drops public + discards private. Decomp absent on the authoring box → vanilla symbols grounded against shipped code (🟢) or flagged VERIFY-AGAINST-DECOMP (🟡); the chest-UI seam is the one real build risk. Carries 5 OPEN knobs for Daniel (tier, Spade-vs-Hammer placement, public size = nomap Q6, private size, destroy-warning hover). Net-new, NOT in requirements.md v1. Card t_b6d133e1 |
| ancient-portal-placeholder-art.md | PROPOSED placeholder art: doctrine-clean kitbash for the Ancient Portal (horizontal overhead disc on root-pillars) from verified vanilla parts — small_portal ring + Greydwarf_Root tangle + stubbe legs, all additive (ADR-0006), scale-lerp grow fake. Art brief for the impl card. PLUS a PARKED art-direction variant (issue 8, t_d6b08678): Daniel's idea-stage musing to drop the wooden ring for 2 pillars + manifested energy — captured with code-coupling map + open questions, NOT greenlit |
| twisted-portal-distinct-mesh.md | PROPOSED (architect, card t_4ab58b42, 2026-06-26): art-brief + spec-delta promoting the Twisted Portal's distinct mesh from deferred art-pass to REQUIRED after Daniel's Niflheim playtest. 🔴 LOAD-BEARING: Daniel is colorblind and the Twisted/Ancient diff today is a green-hue MPB tint ONLY — invisible to him, so the two portals read as fully identical; the fix MUST differentiate by SHAPE/silhouette (monochrome-legible), not hue. 🟢 KEY INSIGHT: the Twisted Portal carries NO TeleportWorld (SBPR_TwistedPortal is the brain), so its ring is purely cosmetic and free to drop — zero functional re-homing, unlike the Ancient Portal whose ring IS TeleportWorld.m_model. Architect lean: drop the small_portal hoop; build 5 uneven inward-leaning tapered SPIKES (Stalagmite `default` mesh, vprefab-verified 0-script/0-collider clean steal) crowned by a self-emissive GuckSack (`Cancer`) mass, r=0.9m, all additive (ADR-0006). Demotes the swamp tint to a secondary cue (still per-renderer MPB, t_f3310406 regression guard). Refined ATs (AT-DISTINCT-SHAPE = Daniel's monochrome by-eye accept), 3 grounded §6 alternatives (HugeRoot1/SwampTree1 swamp-native legs; author-new-mesh; minimal restructure), and the verbatim impl-spec §4.1 replacement to apply in the same PR as the code. Supersedes the generic art-pass deferral for THIS prefab. Gate → engineer-systems impl child card after Daniel picks the direction |
| stone-of-drought-feasibility.md | RESEARCH: feasibility of a Stone of Drought (placeable that carves a curved water divot / repels water). Grounded in vanilla WaterVolume.GetWaterSurface (NOT Rune Magic — clean-room). Key finding: logical water-repel is one Harmony postfix; the VISUAL divot fights a proprietary GPU shader (the hard part). Phased path + Phase-0 spike recommended |
| maritime-exploration-tools.md | BRAINSTORM (idea): v5 Plains maritime exploration tools — Daniel's lighthouse (v3-Beacon promotion, dep t_117bc232) + fog-buoy (🔴 unverified `Floating` buoyancy) seeds, plus the wider set (Star Glass, sea-route/depth/harbor markers, sextant, message-bottle) as prompts only. Grounded vs PARKED:40 / requirements.md:569. No-map/disorientation framing stated; open questions left open for Daniel |
| PARKED-2026-06-03.md | Working-state resume point from the 2026-06-03 session |
| PARKED-2026-06-25.md | **RESUME HERE (latest).** 2026-06-24/25 session: Trailside Camp designed + Bear Hide Tent SHIPPED as a placeholder piece using the vanilla TraderTent mesh via SBPR's FIRST custom AssetBundle. Build 0/0, ALL uncommitted on `main`, NOT verified in-game. #1 next: in-game render verify (bundle load + runtime material only prove out on a joined client). Lists every changed file (incl. which are the sibling beautification thread's, not the tent's), the decomp grounding (sleep gate, Cover, why TraderTent needs a bundle), 5 open recipe/placement knobs, and the next 3 cards (bedroll / camp fire / real art) |
